# BRAINPOOL OS — 샤크 메모리 2026-05-17 (업데이트)

## 0. 읽는 방법
위에서 아래로. 파일 먼저 달라고 하지 말 것.

---

## 1. 프로젝트 정체성
한국어-베트남어 실시간 번역 채팅 플랫폼.
번역 결과를 학습 자산으로 축적하는 언어 생태계.
- 타겟: 50~70대 시니어
- 로그인 없음 (device_id)
- DB 캐싱 우선 → DeepL fallback
- 말풍선: 발신자 기준 (내 메시지=왼쪽)

---

## 2. 레포 구성
```
C:\brainpool-os\           ← 엔진 코어 (로컬 참조용, Vercel 미사용)
C:\brainpool-clean\
  brainpool-clean\         ← Next.js 앱 (메인, Vercel 배포)
    brain-engine/core/     ← brainpool-os 내부 복사본 (Vercel용)
```

---

## 3. 계약서
```
✅ 모든 함수 (ctx) => ctx
✅ throw 금지 → _error 반환
✅ req.text() + JSON.parse()
✅ DB 접근은 connectors/storage.js 통해서만
✅ traceId 항상 포함
❌ 레이어끼리 내부 HTTP 호출
❌ 엔진 안에서 Supabase 직접 createClient
```

---

## 4. 오늘 완료된 작업

### ✅ brainpool-os 통합
- `brain-engine/core/` = brainpool-os 내부 복사
- submodule 문제 해결 → 일반 폴더 변환
- Vercel 배포 성공

### ✅ 구 파일 삭제
```
brain-engine/layers/CoreChatLayer.js        삭제
brain-engine/layers/CoreRingLayer.js        삭제
brain-engine/modules/detectLanguage.js      삭제
brain-engine/modules/translate.js           삭제
brain-engine/modules/emotionFilter.js       삭제
brain-engine/modules/contextFilter.js       삭제
brain-engine/layers/sub/TranslationLayer.js 삭제
brain-engine/layers/sub/LanguageLayer.js    삭제
brain-engine/layers/sub/MeaningLayer.js     삭제
brain-engine/layers/sub/CultureLayer.js     삭제
```

### ✅ 엔진 흐름 검증
```
입력 → createCtx → route('translate') → run()
   → detect → findCache → translate → saveCache  ✅
route('emotion') → analyze()                     ✅
CoreChat: 번역 실패해도 메시지 저장 계속 (fallback) ✅
중복 Translation 경로 제거 확인                   ✅
```

### ✅ BOM 수정
- `brain-engine/core/hajun/router.js` BOM 제거
- `brain-engine/core/engines/translation/index.js` BOM 제거

### ✅ ChatMessageLayer core 이전
- `brain-engine/core/engines/chat/message.js` 신규 작성
- `getSupabase()` 직접 호출 → `connectors/storage.js` 교체
- `module.exports` → ES module `export` 전환

---

## 5. 현재 남은 작업

### 🔴 즉시
- `app/api/chat/send/route.ts` import 교체
  ```
  brain-engine/layers/sub/chat-message-layer
  → brain-engine/core/engines/chat/message.js
  ```
- ChatPresenceLayer 정리 (core 이전 또는 제거)
- `npx next build` 확인
- git push → Vercel 배포

### 🟠 중기
- CoreNull 모듈화
  - 현재 비대한 house.html → 모듈 분리
  - brainpool-os 엔진 위에 올리는 구조
  - CoreRing/CoreChat 연결

### 🟢 장기
- CoreRing Phase 1 (50세션 로그 후)

---

## 6. brain-engine/core 구조 (현재)
```
brain-engine/core/
├── contracts/ctx.js
├── connectors/storage.js
├── hajun/router.js
├── engines/
│   ├── translation/
│   │   ├── index.js
│   │   ├── translate.js
│   │   └── cache.js
│   ├── language/detect.js
│   ├── emotion/analyze.js
│   └── chat/
│       ├── message.js   ← 오늘 신규 (core 이전)
│       ├── room.js
│       └── poll.js
└── package.json
```

---

## 7. DB 테이블
| 테이블 | 용도 |
|--------|------|
| `chat_rooms` | 채팅방 (id=UUID, invite_code=6자) |
| `chat_messages` | 메시지 |
| `tb_trans_logs` | 번역 캐시 (direction: 'ko→vi' or 'vi→ko') |
| `tp_translations` | 방언 사전 |
| `tp_conflicts` | 문화 충돌 단어 |

Supabase URL: `https://grlfocvlfatuvphkyivd.supabase.co`

---

## 8. 핵심 구조 방향
```
brainpool-os / brain-engine/core (엔진 코어)
    ↑
CoreRing / CoreChat (분석/흐름)
    ↑
CoreNull (뷰/표현) ← 다음 모듈화 대상
```

insmeeso 클로 하준아이 5.1작업.md

현실적인 워크플로우 제안
1. 저 → 스펙 문서 작성 (컴팩트하게)
2. 여리 님 → 그록/딥시크에 붙여넣기
3. 그록/딥시크 → 코드 생성
4. 여리 님 → 코드를 저한테 가져오기
5. 저 → 에러 잡기 + 계약서 위반 검사
6. 반복
이 방식이면 제 세션 한도 소모가 훨씬 줄어듭니다. 저는 설계와 검증만 하면 되니까요.

지금 당장 시작할 수 있습니다
HajunAI v5.1 나머지 작업 스펙을 지금 작성해드릴까요?
- ChatGPT/Gemini 셀렉터 스펙
- Gemini 요약 품질 개선 프롬프트 스펙
- contexts upsert 검증 스펙
이걸 그록에게 던지면 바로 코드 나올 수 있게 작성해드리겠습니다.