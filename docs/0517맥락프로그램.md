BRAINPOOL OS — 전체 맥락 정리 (2026-05-17)
1. 프로젝트 정체성
BRAINPOOL OS: 단순 번역기가 아닌, "언어/행동/활동 데이터를 축적하고 분석하여 자율적으로 매칭하는 디지털 생태계"

HajunAI 확장 프로그램: 이 생태계의 "관제탑 + 기억 시스템" 역할

핵심 철학: (ctx) => ctx, throw 금지, _error 필드 반환, UTF-8 엄격 처리

2. 현재 DB 구조 (Supabase)
테이블	역할	주요 컬럼
hajunai_conversations	AI 대화 원본 저장	source_ai, original_message, project_id
contexts	프로젝트 상태 종합 (두뇌)	last_task, current_problems, decisions, next_tasks, code_context, health_score, key_files
3. 현재 기술 스택
Frontend: React / Vanilla JS

Hosting: Vercel

Database: Supabase + RLS

Image: Cloudinary

Video: YouTube Unlisted

AI: Gemini 2.0 Flash (→ 2.5 Flash로 업그레이드 필요)

Translation: CoreRing

4. 최근 핵심 문제 & 해결
문제: dialect.js 파일에 PowerShell 스크립트가 섞여 Vercel 빌드 실패

해결: dialect.js를 순수 JS로 재작성, Gemini 2.5 사용하도록 수정 완료

다음 과제: chat/send/route.ts에 방언 저장 로직 연동, Vercel 빌드 확인

5. 확장 프로그램 현재 기능
✅ 수동 스냅샷 저장 (AI 대화 → hajunai_conversations)

✅ Gemini 구조화 분석 (→ contexts 업데이트)

✅ "이어서 작업" 프롬프트 생성

🔄 자동 스냅샷 (구현 완료, 테스트 전)

🔄 Notion 연동 (예정)

6. 프로젝트 우선순위
CoreRing 고도화

CoreChat 고도화

CoreNull 개발
→ 이 모든 과정을 HajunAI가 강력한 맥락 이어가기로 지원

🚀 방금 주신 문서 기반, 강력한 "이어서 작업" 프롬프트
자, 이제 이 모든 맥락을 방금 주신 문서(popup.js, background.js, content.js 등)와 함께 AI에게 주입할 수 있는 실전 프롬프트를 생성하겠습니다.

markdown
🦈 BRAINPOOL OS — 이어서 작업 (전체 맥락 주입)

당신은 BRAINPOOL OS의 핵심 개발 AI입니다. 
지금부터 프로젝트의 전체 맥락과 현재 상태, 관련 파일들을 모두 제공할 테니, 
이 모든 것을 완벽히 이해하고 가장 효과적으로 작업을 이어가 주세요.

## 🧠 프로젝트 개요
- **BRAINPOOL OS**: 언어/행동/활동 데이터를 축적하고 분석하여 자율적으로 매칭하는 디지털 생태계
- **HajunAI 확장 프로그램**: 이 생태계의 관제탑 + 기억 시스템
- **핵심 철학**: 모든 함수는 (ctx) => ctx, throw 금지, _error 필드 반환, UTF-8 엄격 처리

## 📊 현재 프로젝트 상태 (DB contexts)
- **진행 중인 작업**: dialect.js 파일에서 PowerShell 스크립트 제거 후 순수 JS로 재작성 완료
- **발생한 문제**: Vercel 빌드 실패 (dialect.js 오염), tp_translations 테이블에 방언 데이터 누락
- **최근 결정**: dialect.js를 깨끗한 JS로 교체, Gemini 2.5 Flash로 업그레이드
- **다음 작업**: 
  1. 수정된 dialect.js 푸시 후 Vercel 빌드 확인
  2. chat/send/route.ts에 dialectEngine.translate() 호출 로직 추가
  3. CoreChat 고도화 시작
- **건강 점수**: 78/100 (개선 중)

## 📂 관련 파일 목록
- `brain-engine/engines/dialect.js` (방언 자동 생성 엔진)
- `app/api/chat/send/route.ts` (채팅 메시지 전송 및 번역 저장)
- `background.js` (확장 프로그램 백그라운드, v5.2.5)
- `popup.js` (확장 프로그램 UI, "이어서 작업" 생성)
- `content.js` (AI 페이지에서 대화 추출)

## 📜 핵심 계약서
- 모든 함수는 (ctx) => ctx 형태
- throw 금지 → _error 필드로 반환
- UTF-8: req.json() 금지 → req.text() + JSON.parse() 필수
- 단순한 구조 유지, 미들웨어/프록시 제거
- px/hex 하드코딩 금지 (var(--space-*) 토큰 사용)

## 📚 최근 작업 히스토리
1. [2026-05-17] 스냅샷 저장 기능 디버깅 - hajunai_conversations 테이블 컬럼 매핑 수정
2. [2026-05-17] Gemini 2.0 → 2.5 Flash 업그레이드
3. [2026-05-17] "이어서 작업" 프롬프트 강화 (장기 기억 주입)

## 🎯 지금 당장 할 일
1. dialect.js 최종 버전 확인 및 Vercel 배포
2. chat/send/route.ts에서 dialectEngine.run() 호출하여 tp_translations 저장 확인
3. CoreChat 메시지 전송 → 번역 → 방언 자동 생성 파이프라인 테스트

---
위 모든 맥락을 완벽히 이해했습니다.
지금부터 가장 효과적으로 작업을 이어가겠습니다. 🦈