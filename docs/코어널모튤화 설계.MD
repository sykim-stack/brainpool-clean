현재 구조
app/api/corenull/route.ts       ← API 진입점
brain-engine/
  layers/CoreNullLayer.js       ← 단일 레이어 (모든 액션 처리)
  connectors/storage.js
  contracts/ctx.js
  engines/
    chat/
    dialect/
    emotion/
    language/
    translation/
  hajun/router.js

  목적
CoreNullLayer 모듈화 — 단일 클래스 → 액션별 핸들러 분리
현재 상태
CoreNullLayer.js
  handle()          ← switch 라우터
  getWordData()     ← tp_translations 조회
  getRandomWord()   ← tp_translations 랜덤
  saveWord()        ← tp_translations 삽입
  reportConflict()  ← tp_conflicts 삽입
  resolveConflict() ← tp_conflicts 수정
5개 액션 전부 한 파일에 있어요. 지금 크기는 작지만 CoreHub/CoreRing 붙으면 같은 패턴으로 비대해질 거예요.
모듈화 방향
brain-engine/layers/
  CoreNullLayer.js          ← 라우터만 (handle + dispatch)
  corenull/
    getWordData.js
    getRandomWord.js
    saveWord.js
    reportConflict.js
    resolveConflict.js
흐름
route.ts
  └─ CoreNullLayer.handle(ctx)
       └─ switch → import된 핸들러 함수 호출
            └─ (ctx) => ctx 형식 유지
예외

각 핸들러는 독립 함수 (ctx) => ctx — 클래스 메서드 아님
throw 금지 — _error 반환
traceId ctx에서 그대로 흘러감

운영리스크

route.ts 변경 없음
기존 테스트 36케이스 그대로 통과해야 함